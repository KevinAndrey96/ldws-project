<%= form_for(@the_switch) do |f| %>
  <% if @the_switch.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@the_switch.errors.count, "error") %> prohibited this the_switch from being saved:</h2>

      <ul>
      <% @the_switch.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :switches_id %><br>
    <%= f.text_field :switches_id %>
  </div>
  <div class="field">
    <%= f.label :ports_id %><br>
    <%= f.text_field :ports_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
