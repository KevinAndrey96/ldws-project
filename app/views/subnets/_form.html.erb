<%= form_for(@subnet) do |f| %>
  <% if @subnet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subnet.errors.count, "error") %> prohibited this subnet from being saved:</h2>

      <ul>
      <% @subnet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="col-md-4"></div>
<div class="col-md-4">
  <div class="field">
    <h3>Nombre</h3>
    <%= f.text_field :name, :required => "true", :class => "form-control" %>
  </div>
  <div class="field">
    <h3>Descripción</h3>
    <%= f.text_field :description, :required => "true", :class => "form-control"  %>
  </div>
  <div class="field">
    <h3>Computadores</h3>
    <%= f.number_field :computers, :required => "true", :class => "form-control"  %>
  </div>
  <div class="field">
    <input type="hidden" name="subnet[logical_id]" id="subnet[logical_id]" value="<%= params[:id]%>">
  </div>
  <div class="actions">
    <input type="submit" value="Agregar" class="btn btn-default">
  </div>
<% end %>
</div>
<div class="col-md-4"></div>
